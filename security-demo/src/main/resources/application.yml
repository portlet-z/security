spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc://mysql://127.0.0.1:3306/demo?userUnicode=yes&characterEncoding=UTF-8
    username: root
    password: zhang

  session:
    store-type: none

portlet:
  security:
    browser:
      loginType: JSON
#      loginPage: /demo-signIn.html
